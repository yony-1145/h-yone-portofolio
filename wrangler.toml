# Cloudflare Workers（静的アセット + Astro の _worker.js）
# Astro の API / プリレンダー以外はこの Worker が配信する
name = "h-yone"
main = "./dist/_worker.js/index.js"
compatibility_date = "2026-04-10"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = "./dist"
binding = "ASSETS"
